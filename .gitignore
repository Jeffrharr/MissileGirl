Source/.vs/*
bin/
MissileGirl.zip
obj/
Source/.mfractor/
# Visual Studio left-overs
*.suo        # 'user' settings like 'which file is open in Visual Studio'
*.ncb        # Used for debugging
*.user
*.cache
*.pdb
*.vs
.DS_Store
*.dds
metrics/

# Built mod DLL — regenerated by `dotnet build`; will eventually be published
# by a versioned CI build/Steam publish step instead of being tracked in git.
1.6/Plugins/Stable/Gagarin.dll

# Live test harness generates this from Change_RunA/B.xml per run.
TestMods/TestMod_Change/Patches/Change.xml

# CheckMyVibe — local gate tooling (vendored per developer, not committed)
.checkmyvibe/

# Memtrace — local workspace index, not committed
.memdb/
